name: pull_request.synchronize
on:
  pull_request:
    types:
      - synchronize
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - name: first
      run: |
        echo "Ref: $GITHUB_REF"
        echo "Sha: $GITHUB_SHA"
