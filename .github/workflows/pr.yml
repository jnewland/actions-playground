name: pull_request.synchronize
on:
  pull_request:
    types:
      - labeled
      - unlabeled
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - name: second
      run: |
        echo "Ref: $GITHUB_REF"
        echo "Sha: $GITHUB_SHA"
